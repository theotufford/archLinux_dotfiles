general {
    lock_cmd = hyprlock
    before_sleep_cmd = hyprlock
    after_sleep_cmd = loginctl unlock-session
}

listener {
    timeout =
    on-timeout = hyprlock
}

listener {
    timeout =
    on-timeout = systemctl suspend
}
